# Database Configuration
DATABASE_URL=sqlite+aiosqlite:///./query_parser.db
# For PostgreSQL (when migrating to cloud):
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/query_parser

# Logging
LOG_LEVEL=INFO
LOG_FORMAT="%(asctime)s - %(name)s - %(levelname)s - %(message)s"